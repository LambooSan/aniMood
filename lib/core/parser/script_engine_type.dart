/// Среда исполнения, в которой работает конкретный [SourceModule].
///
/// Выбор движка — свойство модуля, а не глобальная настройка приложения
/// (docs/architecture.md, п.3, "Движки исполнения").
enum ScriptEngineType {
  /// Простые сайты со статичной HTML-разметкой.
  regex,

  /// Источники с динамическим контентом/логикой.
  javascript,

  /// Модули, портированные из Lua-экосистем.
  lua,
}
